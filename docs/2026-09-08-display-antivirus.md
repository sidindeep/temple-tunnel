# Проверка предупреждения антивируса и мигания экрана

SHA-256 файла `.cache/hysteria-official/hysteria.exe`:
`807ae5332a8fefeeff804923e057b6942acceaa11706ac7fa3db42f192c2fde1`.
Совпадает с digest Windows amd64 в официальном GitHub-релизе Hysteria
`app/v2.12.2` (проверено через GitHub API 2026-09-08).
Это подтверждает совпадение с релизом, а не отсутствие всех возможных рисков.
Файл использовался в `tools/auth-isolation.js` для сравнительной диагностики;
приложение и установщик его не используют. Для Hysteria 2 приложение запускает
sing-box. Ненужный диагностический executable удалён из кэша; режим `--official`
диагностического скрипта теперь требует отдельного получения этого файла.
Исключения антивируса не добавлялись.

В системном журнале Windows 2026-09-07 в 13:58:49, 14:00:11 и 14:00:37
есть события Display 4101: видеодрайвер nvlddmkm перестал отвечать и восстановился.
Причинная связь с VPN не установлена. Запуски VPN-ядер используют windowsHide,
CoreHost создаёт консоль с STARTF_USESHOWWINDOW/SW_HIDE.

Отключено аппаратное ускорение Electron до создания окон как мера снижения
риска проблем с отрисовкой. Это не исправление драйвера NVIDIA и не доказательство
устранения мигания: требуется повторная проверка на мониторе после перезапуска.
